apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: persistent-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 16000Gi
  storageClassName: ""
  volumeName: persistent-pv
